name: gestion_academica
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Actualizado
  http: ^1.2.0
  provider: ^6.1.1
  flutter_secure_storage: ^8.0.0
  intl: ^0.18.1

  # ✅ Para subir y ver PDFs (versiones compatibles)
  file_picker: ^8.1.2
  syncfusion_flutter_pdfviewer: ^27.1.48
  syncfusion_flutter_pdf: ^27.1.48
  excel: ^4.0.3
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true